# Accepted campaign artifacts

This directory is tracked and operator-owned. Copy a final report or candidate
here only after replaying the authoritative evaluator state and checking the
evaluator source hash. Codex runtime files belong under `/.autovqe-runtime/`
and must not be promoted merely because an agent claims success.
