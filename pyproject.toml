[project]
name = "autovqe"
version = "0.1.0"
description = "A minimal autonomous VQE research loop"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "qiskit>=1.0",
    "scipy>=1.15",
]

[tool.uv]
package = false
