[project]
name = "autovqe"
version = "0.1.0"
description = "Tiny hardware-aware VQE automation harness"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "qiskit>=1.0",
]

[tool.uv]
package = false
